# Backend API Configuration
# Set this to your actual backend API URL
# NEXT_PUBLIC_ prefix makes it available to both client and server code
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8080

# API key for backend authentication (sent as X-API-Key header)
# Keep this server-side only (no NEXT_PUBLIC_ prefix) for security
BACKEND_API_KEY=your-api-key
