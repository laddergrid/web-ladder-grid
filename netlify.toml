[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"

# Environment variables for your backend API
# Set BACKEND_API_URL in Netlify dashboard under Site settings > Environment variables
